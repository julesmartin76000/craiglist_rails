<h1>Listing categories</h1>

<table>
  <tr>
    <th>Title</th>
  </tr>

  <% @categories.each do |category| %>
    <tr>
      <td><%= category.name %></td>
    <!--   <td><%#= link_to 'Show', article_path(article) %></td> -->
    </tr>
  <% end %>
</table>
